import styled from "styled-components";

export const Content = styled.div`
  display: flex;
  flex-direction: row;
  justify-content: space-between;

  .sidebar {
    display: flex;
    flex-direction: row;
  }

  .icons {
    padding: 1rem;
  }

  .menu {
    padding: 1rem 0;
  }
`;
